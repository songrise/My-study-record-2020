#include <stdio.h>
#include <math.h>

int main(int argc, char const *argv[])
{
    signed char s, v;
    s = 128;
    v = -321;
    printf("s = %d\n", s);
    printf("v = %d", v); //modular 256

    return 0;
}
